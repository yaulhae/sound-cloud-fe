import styled from "styled-components";
import React from "react";
import Template from "../common/Template";

const StreamPageBlock = styled.div``;

const StreamPage = () => {
  return (
    <Template>
      <StreamPageBlock>스트리밍 페이지입니다</StreamPageBlock>
    </Template>
  );
};

export default StreamPage;
