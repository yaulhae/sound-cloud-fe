import styled from "styled-components";
import React from "react";
import Template from "../common/Template";

const MainPageBlock = styled.div``;

const MainPage = () => {
  return (
    <Template>
      <MainPageBlock>메인페이지입니다</MainPageBlock>
    </Template>
  );
};

export default MainPage;
